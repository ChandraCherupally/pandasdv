[build-system]
requires = ["setuptools>=65.5.0", "wheel", "setuptools_scm[toml]>=7.0.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
